#include <stdio.h>

/**
 * main - leadstoinfinite loop
 * Return: 0
 * by martins
*/

int main(void)
{
	printf("commencing infinite loop......\n")
	i = 0;

	while (i < 10)
	{
		putchar(i);
	}

	printf("infiniteloop escaped \\o/\n");
	return (0);
}
