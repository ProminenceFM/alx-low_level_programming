#include "main.h"
/**
 * print_last_digit - print the last digit of a number
 * @n: number whose large digit should be printed
 * by folarin
 * Return:0 for success
*/

int print_last_digit(int n)
{
	int r;

	r = n % 10;
	_putchar(r);
}
