#include"main.h"

/**
 * _abs - print absolute value of an integer
 * @n: the value to be checked
 * by folarin
 * Return: 0 always
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
